{% extends "BlogBundle::layout.html.twig" %}

{% block title %}
  Lecture d'un article - {{ parent() }}
{% endblock %}

{% block blog_body %}
  {% include "BlogBundle:Blog:article.html.twig" %}

  <p>
    <a href="{{ path('blog_accueil') }}" class="btn btn-primary">
      <i class="icon-chevron-left"></i>
      Retour à la liste
    </a>
    {% if is_granted('ROLE_AUTEUR') %}
    <a href="{{ path('blog_modifier', {'id': article.id}) }}" class="btn btn-primary">
      <i class="icon-edit"></i>
      Modifier l'article
    </a>
    <a href="{{ path('blog_supprimer', {'id': article.id}) }}" class="btn btn-primary">
      <i class="icon-trash"></i>
      Supprimer l'article
    </a>
          {% endif %}
  </p>

{% endblock %}